---
# Per environment values which override defaults in hmpps-probation-in-court-automation-tests-service/values.yaml

generic-service:
  replicaCount: 2

  ingress:
    host: probation-in-court-automation-tests-service-test.hmpps.service.justice.gov.uk

  env:
    APPLICATIONINSIGHTS_CONFIGURATION_FILE: "applicationinsights.test.json"
    HMPPS_AUTH_URL: "https://sign-in-test.hmpps.service.justice.gov.uk/auth"

# CloudPlatform AlertManager receiver to route prometheus alerts to slack
# See https://user-guide.cloud-platform.service.justice.gov.uk/documentation/monitoring-an-app/how-to-create-alarms.html#creating-your-own-custom-alerts
generic-prometheus-alerts:
  alertSeverity: hmpps-probation-in-court-nonprod
